import { Sono } from 'next/font/google'

export const sono = Sono({
  subsets: ['latin'],
  display: 'swap',
  weight: '300',
})
export const sono_bold = Sono({
  subsets: ['latin'],
  display: 'swap',
  weight: '600',
})